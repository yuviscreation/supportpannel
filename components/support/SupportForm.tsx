// SupportForm has been removed per repository change to external forms.
// The original implementation was kept here as a placeholder for history
// but we no longer export a React component to avoid accidental usage.

export const SupportForm_removed = true;
